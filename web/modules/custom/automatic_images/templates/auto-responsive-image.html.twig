{% block content %}
  <img
    data-src="{{ default }}"
    data-srcset="{{ srcset }}"
    src="{{ placeholder }}"
    srcset="{{ placeholder }}"
    data-sizes="auto"
    alt="{{ alt }}" class="img-fluid lazyload blur-up"/>
{% endblock %}
